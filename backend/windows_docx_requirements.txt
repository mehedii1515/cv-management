# Windows-compatible requirements for DOCX to PDF conversion
# Install these packages for better Windows compatibility

# Essential packages
python-docx==1.1.2
reportlab==4.2.2
pywin32>=307

# Optional (try if LibreOffice doesn't work)
docx2pdf==0.1.8

# For HTML extraction method
mammoth==1.6.0
